kind: Service
apiVersion: v1
metadata:
  name: service-06-beta
  namespace: default
  labels:
    app: rs-06-beta
    case: minik8s-acceptance-06
spec:
  type: ClusterIP
  selector:
    matchLabels:
      app: rs-06-beta
      case: minik8s-acceptance-06
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
