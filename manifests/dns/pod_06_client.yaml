kind: Pod
apiVersion: v1
metadata:
  name: pod-06-client
  namespace: default
  labels:
    app: pod-06-client
    case: minik8s-acceptance-06
spec:
  nodeSelector:
    node: node-a
  containers:
  - name: client
    image: busybox
    imageTag: 1.36
    command: ["sh", "-c"]
    args: ["sleep 3600"]
  restartPolicy: Always
