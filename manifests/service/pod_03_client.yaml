kind: Pod
apiVersion: v1
metadata:
  name: svc-03-client
  namespace: default
  labels:
    app: svc-03-client
    case: minik8s-acceptance-03
spec:
  nodeSelector:
    node: node-a
  containers:
  - name: client
    image: busybox
    imageTag: 1.36
    command: ["sh", "-c"]
    args: ["sleep 3600"]
  restartPolicy: Always
