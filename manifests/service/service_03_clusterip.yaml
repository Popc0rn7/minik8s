kind: Service
apiVersion: v1
metadata:
  name: svc-03-clusterip
  namespace: default
  labels:
    app: svc-03-nginx
    case: minik8s-acceptance-03
spec:
  type: ClusterIP
  selector:
    matchLabels:
      app: svc-03-nginx
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
